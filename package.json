{
  "name": "audioteka-provider",
  "version": "1.2.1",
  "description": "Audioteka provider",
  "main": "server.js",
  "scripts": {
    "build": "esbuild server.js --platform=node --bundle --outdir=dist"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "cheerio": "^1.1.0",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
